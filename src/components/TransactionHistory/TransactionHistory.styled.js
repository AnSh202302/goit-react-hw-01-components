import { styled } from 'styled-components';

export const TrasactionStyled = styled('table')(() => {
  return {
    margin: '0 auto',
  };
});
export const TrasactionTableTitle = styled('th')(() => {
  return {
    width: 200,
    backgroundColor: '#c1bbbb',
    padding: 8,
    fontWeight: 600,
    border: '1px solid',
  };
});
